name: blank

on:
  push:

jobs:
  everyday:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tremendous: ["reason", "visual", "positive", "emperor", "refresh"]
        compress: ["robot", "vacuum", "flourish", "cheerful"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar IV
